
{
  "rules": {
    ".read": true,
    ".write": true,
    "sms_transactions": {
      ".indexOn": ["parsed_at", "matched_client_id", "status"]
    },
    "transactions": {
      ".indexOn": ["createdAt", "clientId", "status"]
    },
    "cash_receipts": {
      ".indexOn": ["clientId", "status"]
    },
    "cash_payments": {
      ".indexOn": ["clientId"]
    },
    "logs": {
      ".indexOn": ["entityId", "timestamp"]
    }
  }
}
